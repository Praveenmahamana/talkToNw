# Core framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Data layer
duckdb>=0.9.2
pandas>=2.1.0

# Validation & settings
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Time & timezone
pytz>=2023.3

# Configuration
pyyaml>=6.0.1

# Google Auth + Vertex AI (service account REST — no SDK needed)
google-auth>=2.23.0
requests>=2.31.0
python-dotenv>=1.0.0

# Logging
loguru>=0.7.2

# HTTP client (for testing)
httpx>=0.25.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Rich output (optional CLI debugging)
rich>=13.6.0

# Knowledge Graph (in-memory property graph over DuckDB)
networkx>=3.2.1
